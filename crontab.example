# 定时任务配置文件
# 用于自动更新插件内容

# 每天凌晨2点更新
# 0 2 * * * cd /path/to/ui-generator-plugin && ./scripts/update.sh >> logs/update.log 2>&1

# 每周一凌晨2点更新
# 0 2 * * 1 cd /path/to/ui-generator-plugin && ./scripts/update.sh >> logs/update.log 2>&1

# 使用方法：
# 1. 编辑 crontab: crontab -e
# 2. 添加上述任务之一（去掉注释符号 #）
# 3. 修改路径为实际插件路径
# 4. 保存并退出

# 注意：
# - 确保脚本有执行权限: chmod +x scripts/update.sh
# - 确保日志目录存在: mkdir -p logs
# - 可以运行 crontab -l 查看当前任务
